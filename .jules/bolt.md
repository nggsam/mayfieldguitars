## 2024-01-04 - jQuery Dependency Removal
**Learning:** Legacy themes often include heavy dependencies (like jQuery) for trivial functionality (like a simple toggle).
**Action:** Always audit `scripts.js` or main JS files in legacy themes. Often, 90KB+ libraries can be replaced with 10 lines of Vanilla JS.
